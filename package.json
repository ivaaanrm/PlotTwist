{
  "name": "plottwist",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter frontend dev",
    "dev:backend": "docker compose up -d && docker compose watch",
    "dev:all": "docker compose up -d && docker compose watch & bun run dev",
    "lint": "bun run --filter frontend lint"
  }
}
